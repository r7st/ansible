---
- name: Reboot for IPA
  reboot:
  when: ipa
- include: auditd.yml
  tags: auditd
- include: zabbix_agent.yml
  tags: zabbix_agent
- include: selinux.yml
  tags: enable_selinux
- include: sudoers.yml
  when: ipa
  tags: sudoers
- include: ipa.yml
  when: ipa
  tags: ipa
